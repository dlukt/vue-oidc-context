/**
 * @dlukt/vue-oidc-context/router — vue-router integration entry.
 *
 * Placeholder: createAuthGuard (docs/SPEC.md §4.5) lands in milestone M3
 * (docs/PLAN.md). This export exists only so the M0 scaffold emits the
 * dist/router.* files the package.json exports map points to.
 */
export const ROUTER_SCAFFOLD_PLACEHOLDER = true;
