name: Release

on:
  push:
    branches: [main]

concurrency: release-${{ github.ref }}

permissions:
  contents: write # version PR commits, GitHub releases
  pull-requests: write # open/update the version PR
  id-token: write # npm provenance

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      # npm >= 11.5.1 is required for OIDC trusted publishing; older bundled
      # versions fail with a misleading E404 on scoped packages. Pinned to the
      # 11.x line: npm 12 array-wraps `npm info --json` and reshapes error
      # JSON, which @changesets/cli <= 2.31.0 misparses — it then attempts a
      # duplicate publish and crashes in isAlreadyPublishedError.
      - run: npm install -g npm@11
      # v2 is required by @changesets/cli 3: v1 discovered published packages by
      # scraping "New tag:" lines out of `changeset publish` stdout, which 3.x no
      # longer prints. v1 therefore found nothing to release and silently skipped
      # pushing git tags and creating GitHub releases while still exiting 0.
      # v2 reads the structured output file the CLI writes to $CHANGESETS_OUTPUT.
      - name: Create version PR or publish to npm
        uses: changesets/action@v2
        with:
          publish-script: pnpm release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
